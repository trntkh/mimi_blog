<!DOCTYPE html>
<html lang="en">
    <body>
        <!-- Main Content-->
        <div class="container px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <div class="post-preview">
                        <table>
                            <% @articles.each do |article| %>       
                                <tr>
                                    <td>
                                        <h2 class="text-uppercase"><%= link_to article.title, article_path(article) %></h2>
                                        <h3 class="post-subtitle"><%= article.subtitle %></h3>
                                        <h4 class="fst-italic">Posted on <%= article.created_at.strftime('%B %d, %Y') %></h4>
                                        <% if user_signed_in? %>
                                            <%= link_to 'Edit', edit_article_path(article), class: "btn btn-outline-secondary btn-sm rounded-circle" %>
                                            <%= link_to 'Destroy', article_path(article), class: "btn btn-outline-secondary btn-sm rounded-circle",
                                                method: :delete,
                                                data: { confirm: 'Are you sure?' } %>
                                        <% end %>
                                        <hr class="my-4">
                                    </td>
                                </tr>
                            <% end %> 
                        </table>
                    </div>    
                    <div class="d-flex justify-content-end mb-4"><%= link_to 'View All Posts +', {controller: 'articles'}, class: "btn btn-primary text-uppercase" %></div>
                </div>
            </div>        
        </div>
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>
    </body>
</html>
